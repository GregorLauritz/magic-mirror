#!/bin/bash

xset -dpms    # Disable DPMS (Energy Star) features
xset s off    # Disable screensaver
xset s noblank # Disable blanking of the video device
firefox --kiosk {{ backend_url }} &
python3 {{ default_working_dir }}/main.py &
