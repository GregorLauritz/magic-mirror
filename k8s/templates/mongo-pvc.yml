apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-data
  namespace: magic-mirror
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
