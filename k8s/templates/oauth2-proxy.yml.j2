apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy
  namespace: magic-mirror
  labels:
    app: oauth2-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oauth2-proxy
  template:
    metadata:
      labels:
        app: oauth2-proxy
    spec:
      containers:
        - name: oauth2-proxy
          image: {{ registry_url }}/magic-mirror-oauth2-proxy:latest
          imagePullPolicy: Always
          args:
            - --config=/opt/conf/oauth2-proxy.cfg
          ports:
            - containerPort: 443
          envFrom:
            - secretRef:
                name: oauth2-proxy-secrets
          volumeMounts:
            - name: oauth2-proxy-config
              mountPath: /opt/conf
              readOnly: true
            - name: tls-certs
              mountPath: /opt/ssl
              readOnly: true
            - name: root-ca
              mountPath: /etc/rootCA.pem
              subPath: rootCA.pem
              readOnly: true
          livenessProbe:
            httpGet:
              path: /ping
              port: 443
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: 443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
      volumes:
        - name: oauth2-proxy-config
          configMap:
            name: oauth2-proxy-config
        - name: tls-certs
          secret:
            secretName: oauth2-proxy-tls
            items:
              - key: {{ server_fqdn }}.pem
                path: {{ server_fqdn }}.pem
              - key: {{ server_fqdn }}.key
                path: {{ server_fqdn }}.key
        - name: root-ca
          secret:
            secretName: oauth2-proxy-tls
            items:
              - key: rootCA.pem
                path: rootCA.pem
---
apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: magic-mirror
spec:
  type: NodePort
  selector:
    app: oauth2-proxy
  ports:
    - port: 443
      targetPort: 443
      nodePort: 30443
